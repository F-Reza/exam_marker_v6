@extends('layouts.app') @section('title',$student->exists?'Edit Student':'Add Student') @section('page-title','Student Details') @section('content')
<div class="form-page"><form class="panel form-card" method="post" action="{{$student->exists?route('students.update',$student):route('students.store')}}">@csrf @if($student->exists)@method('PUT')@endif<h1>{{$student->exists?'Edit':'Add'}} Student</h1><p>Student details can be linked to written answer papers.</p><div class="form-grid"><label>Student name<input class="input" name="name" value="{{old('name',$student->name)}}" required></label><label>Roll number<input class="input" name="roll_number" value="{{old('roll_number',$student->roll_number)}}"></label><label class="span-2">Grade/Class<input class="input" name="grade" value="{{old('grade',$student->grade)}}"></label>@if(auth()->user()->hasFeature('parent_details'))<label>Parent name<input class="input" name="parent_name" value="{{old('parent_name',$student->parent_name)}}"></label><label>Parent email<input class="input" type="email" name="parent_email" value="{{old('parent_email',$student->parent_email)}}"></label><label>Parent mobile<input class="input" name="parent_mobile" value="{{old('parent_mobile',$student->parent_mobile)}}"></label><label>Alternate contact<input class="input" name="alternate_contact" value="{{old('alternate_contact',$student->alternate_contact)}}"></label>@else<div class="info-note span-2">Parent email and mobile are available only in Mode 3.</div>@endif</div><div class="form-actions"><a class="btn ghost" href="{{route('students.index')}}">Cancel</a><button class="btn">Save Student</button></div></form></div>@endsection